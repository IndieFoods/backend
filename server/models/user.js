class User {
  constructor(uid, name, address, email, role) {
    this.uid = uid;
    this.name = name;
    this.address = address;
    this.email = email;
    this.role = role;
  }
}

export default User;
